import { NgModule } from "@angular/core";
import { AppContador } from "./Contadores/Contador.component";

@NgModule({
    declarations: [
        AppContador
    ],

    exports: [
        AppContador
    ],

})

export class contadorModule { }